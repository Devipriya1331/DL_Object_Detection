# DL_Object_Detection
A real-time object recognition system using OpenCV and MobileNet-SSD that detects and classifies objects from a live webcam feed, displaying bounding boxes and confidence scores efficiently.
